{
  "license": "",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "run-s clean build-only",
    "preview": "vite preview",
    "build-only": "tsc && vite build",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "pixi.js": "^8.0.5"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5",
    "typescript": "^5.4.4",
    "vite": "^5.2.8"
  }
}
